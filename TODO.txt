BACKLOG

INFRASTRUCTURE

[High] Session persistence — sessions are in-memory only and lost on restart.
      Implement JSON serialisation of SessionState to disk (or SQLite), load on
      startup. SessionState is already an immutable record with Jackson annotations,
      so serialisation cost is low. Required before any production deployment.

[Done] Player authorisation — implemented via per-seat playerToken issued at
      lobby join / session creation. SessionHttpServer validates the token before
      dispatching every command. Bot-only sessions bypass token checks.

[Done] Human-only E2E test — HttpApiE2EGameTest.threeHumanPlayerGameProgressesViaHttpApi
       drives a full 3-player game through the HTTP API with only explicit commands
       (no bots). Covers the complete request/response lifecycle.

